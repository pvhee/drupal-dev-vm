---
# The core version you want to use (e.g. 6.x, 7.x, 8.x).
drupal_core_version: "7.x"

# Your Drupal site name.
drupal_site_name: "D7 Test"

# The resulting domain will be [domain].dev (with .dev appended).
domain: "drupaltest"